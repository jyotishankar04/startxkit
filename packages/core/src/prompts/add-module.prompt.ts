// Prompt orchestration intentionally lives in the CLI packages.
// Core only owns reusable option types and generation behavior.
export type { ModuleLayer, ModuleOptions } from "../types/module-options";
