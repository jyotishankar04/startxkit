import type { Request, Response } from "express";
{{#if hasService}}import { {{pascalName}}Service } from "./{{fileName}}.service";
{{/if}}
export class {{pascalName}}Controller {
  {{#if dependencyInjection}}{{#if hasService}}constructor(private readonly {{camelName}}Service: {{pascalName}}Service) {}
  {{else}}constructor() {}
  {{/if}}{{/if}}
  {{#if crud}}
  {{#if dependencyInjection}}getAll = async (_req: Request, res: Response) =>{{else}}static async getAll(_req: Request, res: Response){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.getAll();{{else}}const data: unknown[] = [];{{/if}}
    return res.status(200).json({ success: true, message: "{{pascalName}} fetched successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}getById = async (req: Request, res: Response) =>{{else}}static async getById(req: Request, res: Response){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.getById(req.params.id);{{else}}const data = { id: req.params.id };{{/if}}
    return res.status(200).json({ success: true, message: "{{singularPascalName}} fetched successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}create = async (req: Request, res: Response) =>{{else}}static async create(req: Request, res: Response){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.create(req.body);{{else}}const data = req.body;{{/if}}
    return res.status(201).json({ success: true, message: "{{singularPascalName}} created successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}update = async (req: Request, res: Response) =>{{else}}static async update(req: Request, res: Response){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.update(req.params.id, req.body);{{else}}const data = { id: req.params.id, ...req.body };{{/if}}
    return res.status(200).json({ success: true, message: "{{singularPascalName}} updated successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}remove = async (req: Request, res: Response) =>{{else}}static async remove(req: Request, res: Response){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.remove(req.params.id);{{else}}const data = { id: req.params.id };{{/if}}
    return res.status(200).json({ success: true, message: "{{singularPascalName}} deleted successfully", data });
  }{{#if dependencyInjection}};{{/if}}
  {{else}}
  {{#if dependencyInjection}}index = async (_req: Request, res: Response) =>{{else}}static async index(_req: Request, res: Response){{/if}} {
    return res.status(200).json({ success: true, message: "{{pascalName}} module ready", data: [] });
  }{{#if dependencyInjection}};{{/if}}
  {{/if}}
}
