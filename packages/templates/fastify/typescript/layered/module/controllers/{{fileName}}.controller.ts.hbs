import type { FastifyReply, FastifyRequest } from "fastify";
{{#if hasService}}import { {{pascalName}}Service } from "../services/{{fileName}}.service";
{{/if}}

export class {{pascalName}}Controller {
  {{#if dependencyInjection}}{{#if hasService}}constructor(private readonly {{camelName}}Service: {{pascalName}}Service) {}
  {{else}}constructor() {}
  {{/if}}{{/if}}
  {{#if dependencyInjection}}getAll = async (_request: FastifyRequest, reply: FastifyReply) =>{{else}}static async getAll(_request: FastifyRequest, reply: FastifyReply){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.getAll();{{else}}const data: unknown[] = [];{{/if}}
    return reply.code(200).send({ success: true, message: "{{pascalName}} fetched successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}getById = async (request: FastifyRequest<{ Params: { id: string } }>, reply: FastifyReply) =>{{else}}static async getById(request: FastifyRequest<{ Params: { id: string } }>, reply: FastifyReply){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.getById(request.params.id);{{else}}const data = { id: request.params.id };{{/if}}
    return reply.code(200).send({ success: true, message: "{{singularPascalName}} fetched successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}create = async (request: FastifyRequest, reply: FastifyReply) =>{{else}}static async create(request: FastifyRequest, reply: FastifyReply){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.create(request.body);{{else}}const data = request.body;{{/if}}
    return reply.code(201).send({ success: true, message: "{{singularPascalName}} created successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}update = async (request: FastifyRequest<{ Params: { id: string } }>, reply: FastifyReply) =>{{else}}static async update(request: FastifyRequest<{ Params: { id: string } }>, reply: FastifyReply){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.update(request.params.id, request.body);{{else}}const data = { id: request.params.id, ...(typeof request.body === "object" && request.body !== null ? request.body : {}) };{{/if}}
    return reply.code(200).send({ success: true, message: "{{singularPascalName}} updated successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}remove = async (request: FastifyRequest<{ Params: { id: string } }>, reply: FastifyReply) =>{{else}}static async remove(request: FastifyRequest<{ Params: { id: string } }>, reply: FastifyReply){{/if}} {
    {{#if hasService}}const data = await {{#if dependencyInjection}}this.{{camelName}}Service{{else}}{{pascalName}}Service{{/if}}.remove(request.params.id);{{else}}const data = { id: request.params.id };{{/if}}
    return reply.code(200).send({ success: true, message: "{{singularPascalName}} deleted successfully", data });
  }{{#if dependencyInjection}};{{/if}}

  {{#if dependencyInjection}}index = async (_request: FastifyRequest, reply: FastifyReply) =>{{else}}static async index(_request: FastifyRequest, reply: FastifyReply){{/if}} {
    return reply.code(200).send({ success: true, message: "{{pascalName}} module ready", data: [] });
  }{{#if dependencyInjection}};{{/if}}
}
