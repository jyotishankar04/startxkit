const {{camelName}} = [];

export class {{pascalName}}Repository {
  {{#if dependencyInjection}}async findAll(){{else}}static async findAll(){{/if}} {
    return {{camelName}};
  }

  {{#if dependencyInjection}}async findById(id){{else}}static async findById(id){{/if}} {
    return { id };
  }

  {{#if dependencyInjection}}async create(payload){{else}}static async create(payload){{/if}} {
    return { id: crypto.randomUUID(), ...(typeof payload === "object" && payload !== null ? payload : {}) };
  }

  {{#if dependencyInjection}}async update(id, payload){{else}}static async update(id, payload){{/if}} {
    return { id, ...(typeof payload === "object" && payload !== null ? payload : {}) };
  }

  {{#if dependencyInjection}}async remove(id){{else}}static async remove(id){{/if}} {
    return { id };
  }
}
