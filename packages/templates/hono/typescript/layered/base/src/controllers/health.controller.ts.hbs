import type { Context } from "hono";
import { HealthService } from "../services/health.service";

export class HealthController {
  static check(context: Context) {
    const data = HealthService.check();
    return context.json({
      success: true,
      message: "Server is healthy",
      data,
    });
  }
}
