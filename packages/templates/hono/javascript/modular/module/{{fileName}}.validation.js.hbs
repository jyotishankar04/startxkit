import { z } from "zod";

export const create{{singularPascalName}}Schema = z.object({}).passthrough();
export const update{{singularPascalName}}Schema = create{{singularPascalName}}Schema.partial();
